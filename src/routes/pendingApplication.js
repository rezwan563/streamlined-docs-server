const router = require("express").Router()
const Profiles = require("../models/pendingApplication");

// created profile edited info
router.post("/", async(req,res)=>{

})
// get profile edited info
module.exports = router